type: task

name: chat-ui

image: dstackai/dind
privileged: true

ports:
  - 9000

env:
  - MODEL_ID=meta-llama/Llama-3.2-3B-Instruct
  - HF_TOKEN

commands:
  - start-dockerd
  - docker compose up

resources:
  gpu: 'nvidia:16GB..'
