type: service

name: chat-ui

image: dstackai/dind
privileged: true

port: 9000
auth: false

env:
  - MODEL_ID=meta-llama/Llama-3.2-3B-Instruct
  - HF_TOKEN

commands:
  - start-dockerd
  - docker compose up

resources:
  gpu: 'nvidia:16GB..'
